type Hashtag {
  id: Int!
  name: String!
}

type HashtagWithCount {
  id: Int!
  name: String!
  postCount: Int!
}

extend type Query {
  hashtag(id: Int!): Hashtag
  hashtags(limit: Int, offset: Int): [HashtagWithCount!]!
}
