type Hashtag {
  id: Int!
  name: String!
}

type HashtagWithCount {
  id: Int!
  name: String!
  postCount: Int!
}

extend type Query {
  hashtag(id: Int!): Hashtag
  hashtags: [Hashtag!]!
  popularHashtags(limit: Int): [HashtagWithCount!]!
}
