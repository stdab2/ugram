type SearchResults {
  users: [UserUgram!]!
  posts: [Post!]!
  hashtags: [HashtagResult!]!
}

type HashtagResult {
  id: Int!
  name: String!
  postCount: Int!
}

extend type Query {
  search(
    query: String!
    limit: Int = 20
    offset: Int = 0
  ): SearchResults!
}