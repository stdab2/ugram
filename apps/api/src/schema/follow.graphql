extend type Query {
	followers(userId: Int!, limit: Int, offset: Int): [UserUgram!]!
	following(userId: Int!, limit: Int, offset: Int): [UserUgram!]!
	isFollowing(userId: Int!): Boolean!
}

extend type Mutation {
	followUser(userId: Int!): Boolean!
	unfollowUser(userId: Int!): Boolean!
}
