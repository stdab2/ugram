query PopularPosts($limit: Int, $offset: Int) {
	popularPosts(limit: $limit, offset: $offset) {
		id
		description
		imageUrl
		thumbnailUrl
		messageCount
		likeCount
		isLikedByCurrentUser
		author {
			firstName
			lastName
			picture
			userName
		}
		createdAt
		imageKey
		imageStatus
	}
}
