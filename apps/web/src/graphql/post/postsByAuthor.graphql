query PostsByAuthor($authorId: Int!, $limit: Int, $offset: Int) {
	postsByAuthor(authorId: $authorId, limit: $limit, offset: $offset) {
		id
		description
		imageUrl
		createdAt
		messageCount
		author {
			id
			userName
			firstName
			lastName
			picture
		}
	}
}
