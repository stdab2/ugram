query Post($id: Int!) {
	post(id: $id) {
		id
		description
		thumbnailUrl
		messageCount
		likeCount
		isLikedByCurrentUser
		author {
			id
			firstName
			lastName
			picture
			userName
		}
		createdAt
		imageKey
		imageStatus
	}
}
