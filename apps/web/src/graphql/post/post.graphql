query Post($id: Int!) {
	post(id: $id) {
		id
		description
		imageUrl
		thumbnailUrl
		messageCount
		likeCount
		isLikedByCurrentUser
		author {
			id
			firstName
			lastName
			picture
			userName
		}
		createdAt
		imageKey
		imageStatus
	}
}
