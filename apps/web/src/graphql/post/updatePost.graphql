mutation UpdatePost($id: Int!, $description: String!) {
	updatePost(id: $id, description: $description) {
		id
		description
		imageUrl
		thumbnailUrl
		authorId
		likeCount
		isLikedByCurrentUser
		author {
			id
		}
		createdAt
		imageKey
		imageStatus
	}
}
