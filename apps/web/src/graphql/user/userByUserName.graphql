query UserByUserName($userName: String!) {
	userByUserName(userName: $userName) {
		id
		userName
		email
		firstName
		lastName
		phoneNumber
		picture
		createdAt
		posts {
			id
			imageUrl
			description
			createdAt
		}
	}
}
