query UserByUserName($userName: String!) {
	userByUserName(userName: $userName) {
		id
		userName
		firstName
		lastName
		picture
		createdAt
		posts {
			id
			imageUrl
			description
			createdAt
		}
	}
}
