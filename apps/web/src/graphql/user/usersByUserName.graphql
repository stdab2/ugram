query UsersByUserNames($userNames: [String!]!) {
  usersByUserNames(userNames: $userNames) {
    users {
      id
      userName
      email
      firstName
      lastName
      phoneNumber
      picture
    }
    missingUserNames
  }
}