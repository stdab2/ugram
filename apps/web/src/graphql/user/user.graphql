query User($id: Int!) {
	user(id: $id) {
		id
		userName
		email
		firstName
		lastName
		phoneNumber
		picture
		followerCount
		createdAt
		posts {
			id
			thumbnailUrl
			description
			createdAt
			messageCount
			likeCount
			isLikedByCurrentUser
			imageStatus
		}
	}
}
