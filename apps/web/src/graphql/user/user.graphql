query User($id: Int!) {
	user(id: $id) {
		id
		userName
		email
		firstName
		lastName
		phoneNumber
		picture
		createdAt
		posts {
			id
			imageUrl
			thumbnailUrl
			description
			createdAt
			messageCount
			likeCount
			isLikedByCurrentUser
			imageStatus
		}
	}
}
